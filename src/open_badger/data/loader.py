# Orchestrate source loading, format reading, sampling, transforms, and collation
# into PyTorch-compatible batches. Model-specific forward-pass formatting belongs
# in BaseRobotModel child classes, not in this loader.

