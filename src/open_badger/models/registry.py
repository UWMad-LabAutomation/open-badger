# Resolve model identifiers such as molmoact2 to model-specific adapters.
# Load the model configuration and return the selected model implementation.
# Backend selection belongs to the training or inference layer, not the registry.
# Add registration and build_model behavior when model adapters are implemented.
