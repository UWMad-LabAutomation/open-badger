# Define the common BaseRobotModel contract used by training and inference.
# Keep model-specific preprocessing, loss computation, and component discovery
# in child classes rather than in the generic trainer.

